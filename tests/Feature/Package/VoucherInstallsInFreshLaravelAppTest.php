<?php

it('installs and boots in a fresh laravel application', function () {
    $this->markTestSkipped('This belongs in an external install/integration harness, not the package-local Pest suite.');
});

it('runs package migrations successfully in a fresh laravel application', function () {
    $this->markTestSkipped('This belongs in an external install/integration harness, not the package-local Pest suite.');
});

it('runs a minimal voucher issuance flow in a fresh laravel application', function () {
    $this->markTestSkipped('This belongs in an external install/integration harness, not the package-local Pest suite.');
});

it('runs a minimal voucher redemption flow using a fake payout provider', function () {
    $this->markTestSkipped('This belongs in an external install/integration harness, not the package-local Pest suite.');
});
